import getXPathIndex from '../../src/xpath/getXPathIndex.js';
import { expect } from 'chai';

describe('getXPathIndex', () => {
  it('should get some tests written', () => {
    expect(getXPathIndex).to.be.a('function');
  });
});
