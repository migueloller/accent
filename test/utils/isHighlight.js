import isHighlight from '../../src/utils/isHighlight.js';
import { expect } from 'chai';

describe('isHighlight', () => {
  it('should get some tests written', () => {
    expect(isHighlight).to.be.a('function');
  });
});
