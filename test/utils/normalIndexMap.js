import normalIndexMap from '../../src/utils/normalIndexMap.js';
import { expect } from 'chai';

describe('normalIndexMap', () => {
  it('should get some tests written', () => {
    expect(normalIndexMap).to.be.a('function');
  });
});
