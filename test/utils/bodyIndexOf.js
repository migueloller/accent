import bodyIndexOf from '../../src/utils/bodyIndexOf.js';
import { expect } from 'chai';

describe('bodyIndexOf', () => {
  it('should get some tests written', () => {
    expect(bodyIndexOf).to.be.a('function');
  });
});
