import normalFind from '../../src/utils/normalFind.js';
import { expect } from 'chai';

describe('normalFind', () => {
  it('should get some tests written', () => {
    expect(normalFind).to.be.a('function');
  });
});
