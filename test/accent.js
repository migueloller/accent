import accent from '../src/accent.js';
import { expect } from 'chai';

describe('accent', () => {
  it('should get some tests written', () => {
    expect(accent).to.not.be.undefined; // eslint-disable-line
  });
});
