import serializeRange from '../../src/range/serializeRange.js';
import { expect } from 'chai';

describe('serializeRange', () => {
  it('should get some tests written', () => {
    expect(serializeRange).to.be.a('function');
  });
});
