import rangeIntersectsNode from '../../src/range/rangeIntersectsNode.js';
import { expect } from 'chai';

describe('rangeIntersectsNode', () => {
  it('should get some tests written', () => {
    expect(rangeIntersectsNode).to.be.a('function');
  });
});
