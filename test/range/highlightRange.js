import highlightRange from '../../src/range/highlightRange.js';
import { expect } from 'chai';

describe('highlightRange', () => {
  it('should get some tests written', () => {
    expect(highlightRange).to.be.a('function');
  });
});
