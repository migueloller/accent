import findRange from '../../src/range/findRange.js';
import { expect } from 'chai';

describe('findRange', () => {
  it('should get some tests written', () => {
    expect(findRange).to.be.a('function');
  });
});
