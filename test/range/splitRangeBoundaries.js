import splitRangeBoundaries from '../../src/range/splitRangeBoundaries.js';
import { expect } from 'chai';

describe('splitRangeBoundaries', () => {
  it('should get some tests written', () => {
    expect(splitRangeBoundaries).to.be.a('function');
  });
});
