import deserializeRange from '../../src/range/deserializeRange.js';
import { expect } from 'chai';

describe('deserializeRange', () => {
  it('should get some tests written', () => {
    expect(deserializeRange).to.be.a('function');
  });
});
